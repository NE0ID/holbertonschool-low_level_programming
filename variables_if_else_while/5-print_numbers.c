#include <stdio.h>

/**
 * main - print all signle digit of base 10
 *
 * Return: 0 on success
 */

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
		printf("%d", i);
	putchar('\n');
	return (0);
}
